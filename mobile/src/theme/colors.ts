/**
 * Placeholder color tokens. Real palette/theme direction comes in the UI
 * pass (Phase 5) — this exists now only so screen stubs have something
 * consistent to reference instead of hardcoded hex values scattered
 * around.
 */
export const colors = {
  background: '#0F1115',
  surface: '#1A1D23',
  text: '#F5F6F8',
  textMuted: '#9AA1AC',
  primary: '#6C63FF',
  border: '#2A2E37',
};
